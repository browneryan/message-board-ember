<div class="container">
  <div class="index-body">
    <div class="row">
      {{#each model.questions as |question|}}
        <h5>Q: {{question.inquiry}}</h5>
        <p>By: {{question.author}}</p>
      {{/each}}
    </div>
  </div>
</div>
